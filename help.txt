1. Create virtual environment
virtualenv venv

2. Start virtual environment
source venv/bin/activate

3. Install all python requirements
pip install -r requirements.txt

4. Start server
dev_appserver.py app.yaml

5. Another terminal. Install all npm requirements
npm install

6. Start webpack watcher
webpack --watch

--

# Deploy live
appcfg.py -A pokegraphs update app.yaml


--

Too many files?
mate /Applications/GoogleAppEngineLauncher.app/Contents/Resources/GoogleAppEngine-default.bundle/Contents/Resources/google_appengine/google/appengine/tools/devappserver2/mtime_file_watcher.py

if '/node_modules/' in dirname or '/venv/' in dirname or 'poke' in dirname: continue
